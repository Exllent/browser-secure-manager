[project]
name = "secure-browser"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "PySide6>=6.7",
    "selenium>=4.20",
]

[project.scripts]
secure-browser = "main:main"

[project.gui-scripts]
secure-browser-gui = "main:main"
